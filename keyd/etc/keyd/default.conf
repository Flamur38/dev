[ids]

*

[main]

# Caps Lock: Esc when tapped, Ctrl when held
capslock = overload(control, esc)

# Left Ctrl: Caps Lock when tapped, Ctrl when held
# (keeps a real Caps Lock available without giving up the bottom-left Ctrl)
leftcontrol = overload(control, capslock)
